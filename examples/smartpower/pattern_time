model = getChartByName(diagram,"SMIB")
addParam(model,"theta")
addLocalVar(model,"clock")
formode m = model.modes {
    addFlow(m,"clock_dot = 1")
}

fortran t = model.trans {
    addGuardLabel(t,"&&","clock > theta")
    addResetLabel(t,"clock = 0")
}




